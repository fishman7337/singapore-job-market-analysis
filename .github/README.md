# GitHub Configuration

This folder contains repository automation and GitHub-native project configuration.

| Path | Purpose |
| --- | --- |
| `workflows/` | Continuous integration and CodeQL security scanning workflows |
| `dependabot.yml` | Scheduled dependency update checks for Python and GitHub Actions |

Keep workflow changes small and validate them with local checks before pushing.
