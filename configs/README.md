# Configuration

This folder stores project configuration files that should be versioned with the repository.

| File | Purpose |
| --- | --- |
| `datasets.yml` | Dataset registry used by the DataOps validation helpers |

Update this folder when source filenames, required columns, or validation rules change.
