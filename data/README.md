# Data Directory

This directory is structured for local DataOps workflows.

## Folders

- `raw/`: immutable source files
- `interim/`: temporary cleaned or reshaped files
- `processed/`: final analysis-ready datasets
- `external/`: supplementary reference data

Dataset files are ignored by Git. Keep only documentation and placeholders in version control.
