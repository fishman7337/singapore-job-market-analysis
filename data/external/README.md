# External Data

Place external reference data here only when it is approved for local project use.

Files in this folder are ignored by Git by default. Document any required external source in `docs/REPRODUCIBILITY.md` or `configs/datasets.yml` instead of committing the dataset.
