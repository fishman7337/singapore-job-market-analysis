# Scripts

This folder contains command-line helpers for repository maintenance and validation.

| File | Purpose |
| --- | --- |
| `split_notebook.py` | Splits and verifies the CA2 notebook section files |
| `run_data_quality.py` | Runs data quality checks against the dataset registry |

Scripts should be deterministic and safe to run from the repository root.
