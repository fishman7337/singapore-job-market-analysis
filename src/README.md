# Source Code

This folder contains the installable Python package for reusable analysis and DataOps logic.

The package source lives under `sg_job_market_analysis/` and is installed in editable mode with:

```bash
python -m pip install -e ".[dev]"
```
