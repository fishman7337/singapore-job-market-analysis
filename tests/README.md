# Tests

This folder contains the pytest suite for the reusable package, DataOps checks, and notebook split verification.

Run tests from the repository root:

```bash
pytest
```

Add or update tests whenever cleaning, feature engineering, validation, or notebook-splitting behaviour changes.
